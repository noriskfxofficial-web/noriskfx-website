{
  "name": "noriskfx-netlify-admin",
  "version": "17.0.0",
  "private": true,
  "description": "No Risk FX static website with Netlify Functions admin API and Netlify Blobs persistence.",
  "scripts": {
    "build": "node -e \"console.log('No build step required for No Risk FX static site')\"",
    "dev": "netlify dev",
    "check": "node --check netlify/functions/api.js"
  },
  "dependencies": {
    "@netlify/blobs": "^10.7.6"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=20"
  }
}